# Problems & Hard-Won Lessons

This is the index for the detailed problem notes in [`problems/`](problems/).
Each linked file preserves the original lesson text; entries are ordered newest first.

| # | Problem | Intersects |
| ---: | --- | --- |
| 1 | [Restoring a session: refusing a newer schema must also disable SAVING, "restored" is not a persisted fact, and a restored `running` row must not spin](problems/001-restoring-a-session-refusing-a-newer-schema-must-also.md) | Persistence schema refusals, transient restore UI state, stale in-flight rows. |
| 2 | [Restart re-init must CHAIN on the serial kernel queue — only the kill/boot itself may run un-chained, or the next submission races the fresh worker's preludes](problems/002-restart-re-init-must-chain-on-the-serial-kernel.md) | Restart ordering, queued worker re-init, post-restart state races. |
| 3 | [AttributeGraph spin: an NSView write that self-invalidates DURING a SwiftUI graph update = an infinite update loop — 100% CPU, frozen app, no crash](problems/003-attributegraph-spin-an-nsview-write-that-self-invalidates-during.md) | SwiftUI/AppKit layout invalidation loops, NSViewRepresentable measurement, plot sizing. |
| 4 | [A wrapping `fixedSize` Text OUTSIDE scroll content + `.windowResizability(.contentMinSize)` = the window's MIN HEIGHT balloons to ~1600pt the moment the view appears](problems/004-a-wrapping-fixedsize-text-outside-scroll-content-windowresizability-contentminsize.md) | macOS content min-size, wrapping helper text, sidebar/window resize explosions. |
| 5 | [Parallel swift-testing + `setenv`: tests that pass config through process-global env race every test that reads it](problems/005-parallel-swift-testing-setenv-tests-that-pass-config-through.md) | Parallel Swift Testing, process-global environment overrides, hermetic config injection. |
| 6 | [SwiftMath in a LIVE tape: memoize normalize+parse, and guard every `MTMathUILabel` property write — `sizeThatFits` runs each layout pass and the `latex` setter re-typesets](problems/006-swiftmath-in-a-live-tape-memoize-normalize-parse-and.md) | SwiftMath performance, parse memoization, guarded MTMathUILabel updates. |
| 7 | [A macOS SwiftUI `.popover` is its OWN KEY WINDOW — keys reach neither it nor your editor; transient pickers over a focused text field must be INLINE same-window overlays](problems/007-a-macos-swiftui-popover-is-its-own-key-window.md) | macOS popover focus, keyboard routing, inline transient pickers. |
| 8 | [THE AUTOMATION ENVIRONMENT EATS Esc — a session-wide filtering event tap consumes keyCode 53 before ANY app sees it; verify Esc via `CGEvent.postToPid`](problems/008-the-automation-environment-eats-esc-a-session-wide-filtering.md) | Automation key interception, Esc verification, CGEvent delivery. |
| 9 | [The V0.7 friendly compiler FLATTENS NEWLINES — route multiline input around it, or raw Sage gets corrupted](problems/009-the-v0-7-friendly-compiler-flattens-newlines-route-multiline.md) | Multiline input routing, friendly compiler boundaries, raw Sage preservation. |
| 10 | [`posix_spawn` children INHERIT the parent's signal dispositions — spawn the worker with `POSIX_SPAWN_SETSIGDEF` + `SETSIGMASK` or interrupts silently die](problems/010-posix-spawn-children-inherit-the-parent-s-signal-dispositions.md) | posix_spawn signal inheritance, SIGINT delivery, worker signal masks. |
| 11 | [Session persistence: a missing artifact is the NORMAL case, paths aren't identity, and peek the schema before decoding](problems/011-session-persistence-a-missing-artifact-is-the-normal-case.md) | Session artifact liveness, path identity, schema peek-before-decode. |
| 12 | [Swift process control: `Foundation.Process` can't `setsid` — drop to `posix_spawn`, and an explicit override must fail loud](problems/012-swift-process-control-foundation-process-can-t-setsid-drop.md) | Swift process groups, posix_spawn vs Foundation.Process, fail-loud overrides. |
| 13 | [Exactness: the Symbolic Ring is uniformly `is_exact()==False` — decide per-kind, and tree-walk symbolic](problems/013-exactness-the-symbolic-ring-is-uniformly-is-exact-false.md) | Exactness policy, Symbolic Ring behavior, tree-walked exactness. |
| 14 | [Numeric precision: clamp the request to the value's own `prec()` or `.n(bits)` raises](problems/014-numeric-precision-clamp-the-request-to-the-value-s.md) | Numeric precision bounds, value precision, safe numeric requests. |
| 15 | [Friendly compiler: the bypass rule is a SPACE boundary, and a definite integral is `symbolic`](problems/015-friendly-compiler-the-bypass-rule-is-a-space-boundary.md) | Friendly compiler parsing edges, bypass boundaries, definite integral kind. |
| 16 | [Symbol table: diff the baseline by OBJECT IDENTITY, not name — Sage exports `n`/`N`/`i`](problems/016-symbol-table-diff-the-baseline-by-object-identity-not.md) | Sage symbol snapshots, baseline diffing, object identity vs names. |
| 17 | [Plot artifacts: macOS `NSImage` loads matplotlib SVG but renders it as a black blob — use PNG](problems/017-plot-artifacts-macos-nsimage-loads-matplotlib-svg-but-renders.md) | Plot artifact formats, SVG rendering failure, PNG selection. |
| 18 | [LaTeX rendering: MathJax-via-JavaScriptCore (LaTeXSwiftUI) fails BRACED sub/superscripts here](problems/018-latex-rendering-mathjax-via-javascriptcore-latexswiftui-fails-braced-sub.md) | LaTeXSwiftUI/MathJax limits, JavaScriptCore rendering failures. |
| 19 | [SwiftMath: no `array` environment; rewrite Sage's matrices; size the NSView or it overlaps](problems/019-swiftmath-no-array-environment-rewrite-sage-s-matrices-size.md) | SwiftMath matrix support, Sage LaTeX normalization, NSView sizing. |
| 20 | [Classifying Sage results: type module lies in three specific ways](problems/020-classifying-sage-results-type-module-lies-in-three-specific.md) | Sage type classification, module-name traps, result-kind mapping. |
| 21 | [Numeric approximation: respect the value's native precision; `digits=` can raise](problems/021-numeric-approximation-respect-the-value-s-native-precision-digits.md) | Approximation API behavior, native precision, digits pitfalls. |
| 22 | [Interrupting Sage: cysignals owns SIGINT and makes C code abortable — don't clobber it](problems/022-interrupting-sage-cysignals-owns-sigint-and-makes-c-code.md) | Interrupt semantics, cysignals, abortable C/Sage computations. |
| 23 | [Restart race: give each worker generation its own reader queue + EOF event](problems/023-restart-race-give-each-worker-generation-its-own-reader.md) | Worker generations, reader queues, restart/EOF race isolation. |
| 24 | [`sage -python` is a bash wrapper — kill the process *group*, not the PID](problems/024-sage-python-is-a-bash-wrapper-kill-the-process.md) | sage-python wrapper processes, process-group cleanup, kill targeting. |
| 25 | [Protocol stdout must use a *private dup'd fd*, and capture must redirect fd 1/2 (not just `sys.stdout`)](problems/025-protocol-stdout-must-use-a-private-dup-d-fd.md) | Worker protocol stdout/stderr capture, private fds, fd redirection. |
| 26 | [Sage value-echo: parse the *preparsed* code with `ast`, and suppress `None`](problems/026-sage-value-echo-parse-the-preparsed-code-with-ast.md) | Sage value echo, preparser AST parsing, suppressing None. |
